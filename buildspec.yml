version: 0.1

phases:
  install:
    commands:
      - echo Nothing to do in the pre_build phase...
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Test started on `date`
      - echo `pwd`
      - cd /home/batchuser
      - mkdir -p ./build_test
      - echo Test completed on `date`
  post_build:
    commands:
      - echo Nothing to do in the pre_build phase...

